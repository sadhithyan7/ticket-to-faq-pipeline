# Frontend Placeholder

This directory is reserved for the v0.dev React application. 
The backend FastAPI server handles CORS for `localhost:3000` and `localhost:5173`. 
Once you generate the UI, place it here and run it via npm.
